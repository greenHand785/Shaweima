using Com.Saidao.Pb;
using UnityEngine;

public class TestRead : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Test t = ConfigManager.Instance.GetXmlConfig<Test>(XmlConfigType.Test);
        //Debug.Log($"名称：{t.Name}, 年龄：{t.Age}");
        //Test t2 = ConfigManager.Instance.GetSheetConfig<Test>(SheetConfigType.Test, 1);
        //Debug.Log($"sheetinfo:{t2.Name},{t2.Age},{t2.Id}");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
